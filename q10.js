"use strict";
// Assignment Question # 11
// Name: Store the names of a few of your friends in a array called names. Print each person's name by accessing each element in the list, one at a time. 
const namingArray = ["Qandeel", "Laiba", "Andleeb", "Anamta"];
for (let i = 0; i < namingArray.length; i++) {
    console.log(namingArray[i]);
}
